#include<stdio.h>
#include<string.h>
int main(){
	char s[100];
	printf("enter the string:");
	scanf("%s",&s);
    if(s[0]=='b'&&s[strlen(s-1)=='a'])
    	printf("accepted");
	else
	printf("not accepted");
return 0;
}
